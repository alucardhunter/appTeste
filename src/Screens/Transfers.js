import React from 'react';
import { View, Text } from 'react-native';


const Transfer = () => {
  return (
    <View style={{flex:1, marginBottom: 100}}>
      <Text style={{alignSelf:'center', marginTop: '10%', fontSize: 20}} >TRANSFERS</Text>
    </View>
  );
}

export default Transfer;